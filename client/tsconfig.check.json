{
  "//": "Typecheck isolado dos módulos puros (sem imports do Vencord). O resto do plugin só compila dentro do Vencord (pnpm build).",
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "strict": true,
    "noEmit": true,
    "skipLibCheck": true,
    "jsx": "react-jsx"
  },
  "include": [
    "src/types.ts"
  ]
}
