{
  "name": "frd-discord-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MCP do FRD GoLive — ponte entre o agente (OpenCode) e o Discord modificado (plugin Vencord).",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.5.0",
    "typescript": "^5.6.0"
  }
}
